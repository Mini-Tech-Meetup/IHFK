# QA evidence — 2026-07-13

## Static and automated checks

```text
> npm run check
PASS 23 source modules are readable
PASS all relative imports and 7 runtime assets exist
PASS 10 locales contain 25 required keys
PASS 11 SPEC documents use the required headings

> git diff --check
PASS (line-ending warnings only)

Browser unit suite (?shareFallback):
22 passed, 0 failed
```

The browser suite covers locale parity/interpolation/button widths, stage counting, empty/depleted weapon selection, weapon caps, timer freezing, locale-preserving restart, the current 25-kiosk street target, kiosk and factory timing with both fist-only and full reserves, mute/resume, successful and rejected mobile fullscreen/orientation APIs, all-locale 1080×640 result-share creation with the loaded character sprite, and PNG-download fallback.

## End-to-end campaign

- Accelerated integration mode traversed Intro → FastFood → Street → Factory → Result without console errors.
- Result showed the frozen time and kiosk count; the factory body was not counted.
- Keep Breaking returned to the broken factory, passed 300 additional destructions, and ran a further 25-second stress interval without console errors.
- Normal-balance automated run completed at `126,790 ms` with `172` kiosks destroyed. This automation attacks the whole screen and is intentionally an optimal lower bound, not an average human result.
- With fist damage `12` and cooldown `100 ms`, a 5,000 HP factory takes approximately `41.7 s` of uninterrupted fist attacks.
- Factory-specific weapon damage keeps a full-reserve route at approximately `36.9 s`, so both fist-only and all-weapon routes remain inside the 35–45 second target.
- Final regression after adding hit-stop, weapon break effects, factory assembly launches, and falling pickups reached Result in accelerated mode, returned `downloaded` from the real Result share action, entered Endless, and completed another 25-second stress interval with no warnings or errors.

## Hosting and responsive checks

- Root local HTTP booted Phaser 4.1.0 with WebGL and Web Audio.
- The public GitHub Pages candidate `https://mini-tech-meetup.github.io/IHFK/` returned HTTP 200; the README now records it while noting that local changes require publication.
- A Pages-equivalent subpath at `/IHFK/` loaded the Language scene with a 1080×640 canvas and no console error.
- At 844×390 landscape, FIT scaling retained the whole canvas with letterboxing and no document overflow.
- Forced-touch landscape showed joystick, jump, attack, mute, and weapon 1–4 controls. Weapon selection and queued tap attack reached the shared `InputController` state.
- Portrait rotation overlay and real Android/iOS fullscreen/orientation behavior remain real-device checks.

## Visual and performance evidence

- Pillow inspection: `factory.png` 1080×640/16 colors, `fastfood.png` 1080×640/16 colors, `street.png` 1080×640/15 colors; all three have alpha range `(255, 255)`.
- The backgrounds are nearest-neighbor enlargements from 270×160 palette-reduced sources.
- Existing character sheets are rendered at 5× scale against the same backgrounds.
- Campaign active-kiosk caps are 12 on Street and 15 in Factory; endless mode increases its cap every 20 seconds to a hard maximum of 30.
- No warning or error was logged during accelerated campaign, 300+ endless destructions, or the 25-second endless stress interval.

## Remaining external checks

- Android Chrome real-device landscape/fullscreen/orientation lock.
- iOS Safari manual-rotation fallback and audio unlock.
- Native Web Share file sheet on a supporting mobile device. The desktop PNG fallback is automated and passing.
- Human play-session sample for the 2:30–3:30 average target.

## Device QA collector

- `/qa/device.html` is available on the same static origin and does not persist or transmit results.
- Desktop smoke confirmed environment detection, five action buttons, a 1080×640 pixelated stress canvas, JSON report generation, and zero console warnings/errors.
- The 10-second 30-kiosk/120-fragment desktop reference run measured average `55.6 FPS` and 5th-percentile `54.1 FPS`. This is a collector smoke result, not mobile acceptance evidence.
- Android/iOS results must be pasted into [DEVICE_QA_TEMPLATE.md](DEVICE_QA_TEMPLATE.md).

## Defects

- P0: 0
- P1: 0
- P2: 0 known in automated coverage
- P3: real-device and human-balance evidence pending
