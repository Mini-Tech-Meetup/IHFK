# Initial implementation evidence — 2026-07-13

## Automated checks

```text
> npm run check
PASS 21 source modules are readable
PASS 10 locales contain 25 required keys

Browser tests at the initial checkpoint:
PASS all locales contain every English key
PASS session counts stages once per call
PASS weapon grants cap at double amount
PASS depleted weapon returns to fist
PASS translation interpolation works
PASS all localized share templates interpolate
PASS timer freezes after stop
PASS reset preserves chosen locale
PASS campaign balance uses 50 street kiosks
PASS fist runs at the required 10 attacks per second
PASS share card renders at 1080 square
PASS share fallback creates a PNG download
12 passed, 0 failed
```

The current expanded suite is recorded in [the QA evidence](2026-07-13-qa.md).

All JavaScript modules also passed `node --check` after enabling ESM through `package.json`.

## Browser smoke

- Phaser v4.1.0 booted with WebGL and Web Audio on local HTTP.
- Language selection rendered all 10 native language names.
- English title menu rendered Language / How To Use / Credits and the randomized start-button bubble.
- The How To Use screen contained a normal-size Game Start button.
- The automated intro transitioned into the fast-food gameplay scene with no console errors.
- HUD displayed store objective, timer, and all four weapon slots.
- Accelerated integration mode reached Result through Intro, FastFood, Street, and Factory with zero console errors.
- Result displayed time, total kiosk count, Retry, Keep Breaking, and Share.
- Keep Breaking entered Endless and increased the extra count beyond 300 without console errors.
- A 25-second endless stress interval completed without warnings or errors.
- At 844×390 landscape viewport, the 1080×640 canvas remained fully visible with FIT letterboxing and no document overflow.

## Art generation

- Built-in image generation used `legacy/assets/market/shop.png`, `legacy/assets/market/kiosk.png`, and the strong-guy attack sheet as style references.
- Generated fast-food, street, and factory backgrounds were normalized to 270×160, reduced to 16 colors without dithering, then enlarged to 1080×640 with nearest-neighbor filtering.
- Final assets are stored in `assets/background/`.

## Known verification gaps

- See [the QA evidence](2026-07-13-qa.md) for subsequent full-campaign, endless, subpath, touch, share-fallback, and balance measurements.
- Android/iOS real-device behavior, native file sharing, and a human play-session sample remain external checks.
